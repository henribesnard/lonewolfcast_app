class BankrollScreen extends StatelessWidget {
  const BankrollScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Bankroll')),
    );
  }
}

